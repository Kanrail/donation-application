import React from 'react';

const Navbar = () => {
    return(
        <div>
            {/* Including for inclusion of other elements possibly needed, will need to be inserted as list elements */}
        </div>
    );
}

export default Navbar;